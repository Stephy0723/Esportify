import 'package:flutter/material.dart';

class TagMultijugadorPage extends StatelessWidget {
  const TagMultijugadorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#Multijugador")),
      body: const Center(child: Text("Contenido para el tag #Multijugador")),
    );
  }
}
