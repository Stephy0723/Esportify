import 'package:flutter/material.dart';

class TagMlbbPage extends StatelessWidget {
  const TagMlbbPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#Mobile Legends: bang bang")),
      body: const Center(child: Text("Contenido para el tag #MLBB")),
    );
  }
}
