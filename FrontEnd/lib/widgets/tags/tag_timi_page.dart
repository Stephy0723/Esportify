import 'package:flutter/material.dart';

class TagTimiPage extends StatelessWidget {
  const TagTimiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#TIMI")),
      body: const Center(child: Text("Contenido para el tag #TIMI")),
    );
  }
}
