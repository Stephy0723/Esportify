import 'package:flutter/material.dart';

class TagMobaPage extends StatelessWidget {
  const TagMobaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#Moba")),
      body: const Center(child: Text("Contenido para el tag #Moba")),
    );
  }
}
