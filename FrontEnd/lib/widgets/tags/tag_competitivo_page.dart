import 'package:flutter/material.dart';

class TagCompetitivoPage extends StatelessWidget {
  const TagCompetitivoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#Competitivo")),
      body: const Center(child: Text("Contenido para el tag #Competitivo")),
    );
  }
}
