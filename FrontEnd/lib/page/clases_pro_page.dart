import 'package:flutter/material.dart';

class ClasesProPage extends StatelessWidget {
  const ClasesProPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Clases Pro',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
