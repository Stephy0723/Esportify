import 'package:flutter/material.dart';

class TransmisionPage extends StatelessWidget {
  const TransmisionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Transmisión',
        style: TextStyle(fontSize: 24, color: Colors.white),
      ),
    );
  }
}
