import 'package:flutter/material.dart';

class TagEstrategiaPage extends StatelessWidget {
  const TagEstrategiaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#Estrategia")),
      body: const Center(child: Text("Contenido para el tag #Estrategia")),
    );
  }
}
