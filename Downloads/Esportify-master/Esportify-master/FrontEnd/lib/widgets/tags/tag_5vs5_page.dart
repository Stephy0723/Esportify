import 'package:flutter/material.dart';

class Tag5vs5Page extends StatelessWidget {
  const Tag5vs5Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("#5vs5")),
      body: const Center(child: Text("Contenido para el tag #5vs5")),
    );
  }
}
